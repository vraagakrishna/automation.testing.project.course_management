name: Mobile Automation Matrix
on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'Branch to run tests on'
        required: true
        default: 'main'

  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - '.gitignore'

  pull_request:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - '.gitignore'

jobs:
  mobile-build:
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform_name: Android
            execution_type: mobileWeb
            name: Android-Web
          - platform_name: Android
            execution_type: nativeApp
            name: Android-Native
          - platform_name: iOS
            execution_type: mobileWeb
            name: iOS-Web

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch_name }}

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: maven

      - name: Install Node.js & Appium
        run: |
          npm install -g appium
          appium driver install uiautomator2

      # --- LOGIC FOR ANDROID ---
      - name: Run Android Tests (${{ matrix.execution_type }})
        if: matrix.platform == 'Android'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 31
          target: google_apis
          arch: arm64-v8a
          script: |
            mvn clean test \
            -DPLATFORM_NAME=${{ matrix.platform_name }} \
            -DEXECUTION_TYPE=${{ matrix.execution_type }} \
            -DADMIN_EMAIL=${{ secrets.ADMIN_EMAIL }} \
            -DADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }} \
            -DUSER_EMAIL=${{ secrets.USER_EMAIL }} \
            -DUSER_PASSWORD=${{ secrets.USER_PASSWORD }}
            
      # --- LOGIC FOR iOS ---
      - name: Run iOS Tests (${{ matrix.execution_type }})
        if: matrix.platform == 'iOS'
        run: |
          # Booting the simulator
          UDID=$(xcrun simctl list devices | grep -m 1 "iPhone 15" | awk -F '[()]' '{print $2}')
          xcrun simctl boot $UDID
          xcrun simctl waitstarted $UDID

          # Run tests (iOS Native will fail as expected via your Java exception)
          mvn clean test \
          -DPLATFORM_NAME=${{ matrix.platform_name }} \
          -DEXECUTION_TYPE=${{ matrix.execution_type }} \
          -DADMIN_EMAIL=${{ secrets.ADMIN_EMAIL }} \
          -DADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }} \
          -DUSER_EMAIL=${{ secrets.USER_EMAIL }} \
          -DUSER_PASSWORD=${{ secrets.USER_PASSWORD }}

      # --- SHARED REPORTING ---
      - name: Upload Test Report (${{ matrix.name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Reports-${{ matrix.name }}
          path: ./Reports/
          if-no-files-found: warn
